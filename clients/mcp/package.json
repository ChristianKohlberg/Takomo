{
  "name": "takomo-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the Takomo HTTP API as native tools for AI agents (Claude Code, Codex, etc.)",
  "license": "MIT",
  "type": "module",
  "bin": {
    "takomo-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node test/e2e.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
